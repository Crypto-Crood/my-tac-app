pkg update && pkg upgrade -y && pkg install -y proot-distro && proot-distro install ubuntu && proot-distro login ubuntu -- bash -c "apt update && apt upgrade -y && apt install -y build-essential pkg-config libssl-dev git-all protobuf-compiler" && source $HOME/.cargo/env && read -p $'\033[1;32mEnter your prover-id: \033[0m' prover_id && mkdir -p ~/.nexus && echo $prover_id > ~/.nexus/prover-id && curl -sL https://cli.nexus.xyz/ | sh
